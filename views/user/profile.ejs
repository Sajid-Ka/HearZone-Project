<%- include('../partials/user/header') %>

<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/noty/3.1.4/noty.min.css">
<script src="https://cdnjs.cloudflare.com/ajax/libs/noty/3.1.4/noty.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" integrity="sha512-Fo3rlrZj/k7ujTnHg4CGR2D7kSs0v4LLanw2qksYuRlEzO+tcaEPQogQ0KaoGN26/zrn20ImR1DfuLWnOo7aBA==" crossorigin="anonymous" referrerpolicy="no-referrer" />

<style>
    body {
        font-family: 'Poppins', sans-serif;
        background-color: #f5f5f5;
        margin: 0px;
        overflow-x: hidden; 
    }

    /* Profile Section */
    .card {
        background: white;
        border: none;
        border-radius: 10px;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
    }

    .card h4 {
        font-weight: 600;
        color: #444;
    }

    .profile-info p {
        color: #666;
    }
    .profile-info .btn-sm {
        padding: 0.25rem 0.75rem;
    }
    
    .profile-info strong {
        color: #222;
        font-weight: 600;
    }

    /* Buttons */
    .btn-dark {
        background-color: #222;
        border-radius: 8px;
        transition: all 0.3s ease;
        color: white;
    }

    .btn-dark:hover {
        background-color: #f1c40f;
        color: #222;
    }

    .btn-primary {
        background-color: #3498db;
        border-radius: 8px;
        transition: all 0.3s ease;
    }

    .btn-primary:hover {
        background-color: #2980b9;
    }

    .btn-danger {
        background-color: #e74c3c;
        border-radius: 8px;
        transition: all 0.3s ease;
    }

    .btn-danger:hover {
        background-color: #c0392b;
    }

    /* Profile Picture */
    .rounded-circle {
        background: #444;
        width: 100px;
        height: 100px;
        display: flex;
        align-items: center;
        justify-content: center;
        color: white;
        font-size: 36px;
        font-weight: 600;
    }

    .profile-image-container {
        position: relative;
        width: 100px;
        height: 100px;
    }
    
    .profile-image {
        width: 100%;
        height: 100%;
        object-fit: cover;
        image-rendering: -webkit-optimize-contrast;
        image-rendering: crisp-edges;
    }
    
    .upload-btn {
        position: absolute;
        bottom: 0;
        right: 0;
        font-size: 12px;
        padding: 2px 6px;
    }
    
    .profile-image-container .upload-btn {
        opacity: 0;
        transition: opacity 0.3s ease;
    }
    
    .profile-image-container:hover .upload-btn {
        opacity: 1;
    }
    .btn-sm {
        padding: 0.25rem 0.5rem;
        font-size: 0.875rem;
    }

    /* Address Card Styling */
    .address-card {
        padding: 15px;
        margin-bottom: 20px;
    }

    .address-card p {
        color: #666;
        margin-bottom: 10px;
    }

    .badge {
        font-size: 12px;
        padding: 5px 10px;
        border-radius: 12px;
    }

    .modal-content {
        border-radius: 10px;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
    }

    .modal-header {
        background-color: #222;
        color: white;
        border-radius: 10px 10px 0 0;
    }

    .modal-title {
        font-weight: 600;
    }

    .btn-close {
        filter: invert(1);
    }

    .form-label {
        font-weight: 500;
        color: #555;
    }

    .form-control {
        border-radius: 8px;
        padding: 12px;
        border: 1px solid #ddd;
    }

    .form-control:focus {
        border-color: #f1c40f;
        box-shadow: 0 0 5px rgba(241, 196, 15, 0.5);
    }

    .invalid-feedback {
        display: none;
        color: #dc3545;
        font-size: 0.875em;
        margin-top: 0.25rem;
    }
    .form-control.is-invalid ~ .invalid-feedback {
        display: block;
    }


    /* Referral Section Styles */
    .referral-section {
        margin-top: 20px;
        padding: 20px;
        background-color: #f8f9fa;
        border-radius: 8px;
        border: 1px solid #dee2e6;
    }

    .referral-code {
        font-family: monospace;
        font-size: 1.2em;
        background-color: #e9ecef;
        padding: 10px;
        border-radius: 5px;
        display: inline-block;
        margin-right: 10px;
    }

    .copy-btn {
        padding: 5px 10px;
        font-size: 0.9em;
    }

    #referralLink {
        background-color: #f8f9fa;
        font-size: 0.9em;
        height: 38px;
    }

    .share-buttons {
        margin-top: 15px;
    }

    .share-btn {
        margin-right: 10px;
        padding: 8px 15px;
        border-radius: 5px;
        font-size: 0.9em;
    }

    .btn-outline-primary {
        color: #25D366;
        border-color: #25D366;
    }

    .btn-outline-primary:hover {
        background-color: #25D366;
        color: white;
    }

    .btn-outline-info {
        color: #0d6efd;
        border-color: #0d6efd;
    }

    .btn-outline-info:hover {
        background-color: #0d6efd;
        color: white;
    }

    /* Footer styles */
    .footer {
        width: 100vw;
        position: relative;
        left: 50%;
        right: 50%;
        margin-left: -50vw;
        margin-right: -50vw;
        background-color: #222;
        color: white;
        padding: 0;
        margin-top: 20px;
    }

    .footer #footer,
    .footer #footer-bottom {
        width: 100%;
        margin: 0;
    }

    .footer #footer .container-fluid,
    .footer #footer-bottom .container-fluid {
        padding-left: 0 !important;
        padding-right: 0 !important;
        margin: 0 !important;
        width: 100% !important;
    }

    .footer #footer .row,
    .footer #footer-bottom .row {
        margin-left: 0 !important;
        margin-right: 0 !important;
        padding-left: 15px !important;
        padding-right: 15px !important;
    }

    .footer #footer-bottom {
        background-color: #111;
    }

    .referral-section {
        margin-top: 20px;
        padding: 15px;
        background-color: #f8f9fa;
        border-radius: 8px;
    }
    .referral-code {
        font-family: monospace;
        font-size: 1.2em;
        background-color: #e9ecef;
        padding: 10px;
        border-radius: 5px;
        display: inline-block;
    }
    .copy-btn {
        margin-left: 10px;
        padding: 5px 10px;
        font-size: 0.9em;
    }
</style>

<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">

<br><br><br><br><br><br>

<%- include('../../views/partials/user/breadcrumb', {
    breadcrumbs: [
        { text: 'Profile', url: '#', active: true }
    ]
}) %>

<div class="container-fluid mt-5">
    <div class="row">
        <%- include('../partials/user/profile/sidebar') %>
        <div class="col-md-9">
            <h2>My Account</h2>
            <div class="card p-4">
                <div class="d-flex align-items-center mb-4">
                    <div class="profile-image-container rounded-circle me-3">
                        <% if (user.profileImage) { %>
                            <img src="/uploads/profile-images/<%= user.profileImage %>" alt="Profile" class="profile-image rounded-circle">
                        <% } else { %>
                            <div class="rounded-circle">
                                <%= user.name ? user.name.charAt(0).toUpperCase() : 'U' %>
                            </div>
                        <% } %>
                        <form action="/update-profile-image" method="POST" enctype="multipart/form-data" id="profileImageForm">
                            <input type="file" name="profileImage" id="profileImage" accept="image/jpeg,image/jpg,image/png,image/gif" class="d-none">
                            <label for="profileImage" class="btn btn-primary upload-btn">Change</label>
                        </form>
                    </div>
                    <div>
                        <h4><%= user.name || 'User Name' %></h4>
                        <p class="text-muted mb-0">Member since <%= user.createdAt ? new Date(user.createdAt).toLocaleDateString() : 'N/A' %></p>
                    </div>
                </div>

                <h4>Profile Details</h4>
                <div class="profile-info mb-3">
                    <p><strong>Full Name:</strong> <%= user.name || 'Not provided' %></p>
                    <p><strong>Email:</strong> <%= user.email || 'Not provided' %></p>
                    <p><strong>Phone:</strong> <%= user.phone || 'Not provided' %></p>
                    <p><strong>Account Status:</strong> <%= user.isBlocked ? 'Blocked' : 'Active' %></p>
                </div>

                <!-- Referral Section -->
                <div class="referral-section">
                    <h4>Refer & Earn</h4>
                    <p>Invite your friends and earn a 10% discount coupon for each successful referral!</p>
                    <div class="mb-3">
                        <% if (user.referralCode && typeof user.referralCode === 'string' && user.referralCode.length === 8) { %>
                            <span class="referral-code"><%= user.referralCode %></span>
                            <button class="btn btn-primary copy-btn" onclick="copyReferralCode()">Copy Code</button>
                        <% } else { %>
                            <span class="text-danger">Referral code not available</span>
                            <button class="btn btn-primary copy-btn" disabled>Copy Code</button>
                        <% } %>
                    </div>
                    <div class="input-group mb-3">
                        <input type="text" class="form-control" id="referralLink" 
                            value="<%= referralLink %>" 
                            <%= !user.referralCode ? 'disabled' : '' %>
                            readonly>
                        <button class="btn btn-dark" onclick="copyReferralLink()" 
                            <%= !user.referralCode ? 'disabled' : '' %>>Copy Link</button>
                    </div>
                    <div class="d-flex gap-2">
                        <button class="btn btn-sm btn-outline-primary" onclick="shareViaWhatsApp()">
                            <i class="fab fa-whatsapp"></i> Share
                        </button>
                        <button class="btn btn-sm btn-outline-info" onclick="shareViaEmail()">
                            <i class="fas fa-envelope"></i> Email
                        </button>
                    </div>
                </div>

            <!-- Updated Saved Addresses Section -->
            <div class="card p-4 mt-4">
                <div class="d-flex justify-content-between align-items-center mb-3">
                    <h4>Saved Addresses</h4>
                    <a href="/address" class="btn btn-primary btn-sm">Add New Address</a>
                </div>
                <% if (addresses && addresses.length > 0) { %>
                    <div class="row">
                        <% addresses.forEach((address, index) => { %>
                            <div class="col-md-6">
                                <div class="card address-card">
                                    <div class="card-body">
                                        <h5 class="card-title"><%= address.name %> 
                                            <% if (address.isDefault) { %>
                                                <span class="badge bg-success">Default</span>
                                            <% } %>
                                        </h5>
                                        <p class="card-text">
                                            <%= address.addressType %><br>
                                            <%= address.landmark %>, <%= address.city %>, <%= address.state %> - <%= address.pinCode %><br>
                                            <strong>Phone:</strong> <%= address.phone %>
                                            <% if (address.altPhone) { %>
                                                <br><strong>Alternate Phone:</strong> <%= address.altPhone %>
                                            <% } %>
                                        </p>
                                        <div class="d-flex justify-content-end gap-2">
                                            <button class="btn btn-dark btn-sm" data-bs-toggle="modal" data-bs-target="#editModal<%= address._id %>">Edit</button>
                                            <form action="/address/delete/<%= address._id %>" method="POST" class="d-inline">
                                                <button type="submit" class="btn btn-danger btn-sm" >Delete</button>
                                            </form>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <!-- Edit Modal -->
                            <div class="modal fade" id="editModal<%= address._id %>" tabindex="-1">
                                <div class="modal-dialog">
                                    <div class="modal-content">
                                        <div class="modal-header">
                                            <h5 class="modal-title">Edit Address</h5>
                                            <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                                        </div>
                                        <div class="modal-body p-4">
                                            <form action="/address/edit/<%= address._id %>" method="POST" class="editAddressForm">
                                                <div class="mb-3">
                                                    <label class="form-label">Full Name</label>
                                                    <input type="text" class="form-control" name="name" value="<%= address.name %>" required>
                                                    <div class="invalid-feedback">Please enter full name</div>
                                                </div>
                                                <div class="mb-3">
                                                    <label class="form-label">Address Type</label>
                                                    <select class="form-control" name="addressType" required>
                                                        <option value="Home" <%= address.addressType === 'Home' ? 'selected' : '' %>>Home</option>
                                                        <option value="Work" <%= address.addressType === 'Work' ? 'selected' : '' %>>Work</option>
                                                        <option value="Other" <%= address.addressType === 'Other' ? 'selected' : '' %>>Other</option>
                                                    </select>
                                                    <div class="invalid-feedback">Please select address type</div>
                                                </div>
                                                <div class="mb-3">
                                                    <label class="form-label">City</label>
                                                    <input type="text" class="form-control" name="city" value="<%= address.city %>" required>
                                                    <div class="invalid-feedback">Please enter city</div>
                                                </div>
                                                <div class="mb-3">
                                                    <label class="form-label">Landmark</label>
                                                    <input type="text" class="form-control" name="landmark" value="<%= address.landmark %>" required>
                                                    <div class="invalid-feedback">Please enter landmark</div>
                                                </div>
                                                <div class="mb-3">
                                                    <label class="form-label">State</label>
                                                    <input type="text" class="form-control" name="state" value="<%= address.state %>" required>
                                                    <div class="invalid-feedback">Please enter state</div>
                                                </div>
                                                <div class="mb-3">
                                                    <label class="form-label">Pin Code</label>
                                                    <input type="text" class="form-control" name="pinCode" value="<%= address.pinCode %>" pattern="\d{6}" required>
                                                    <div class="invalid-feedback">Please enter a valid 6-digit pin code</div>
                                                </div>
                                                <div class="mb-3">
                                                    <label class="form-label">Phone</label>
                                                    <input type="tel" class="form-control" name="phone" value="<%= address.phone %>" pattern="\d{10}" required>
                                                    <div class="invalid-feedback">Please enter a valid 10-digit phone number</div>
                                                </div>
                                                <div class="mb-3">
                                                    <label class="form-label">Alternate Phone (Optional)</label>
                                                    <input type="tel" class="form-control" name="altPhone" value="<%= address.altPhone || '' %>" pattern="\d{10}">
                                                    <div class="invalid-feedback">Please enter a valid 10-digit phone number (must be different from primary phone)</div>
                                                </div>
                                                <div class="mb-3">
                                                    <div class="form-check">
                                                        <input type="checkbox" class="form-check-input" name="isDefault" id="isDefault<%= address._id %>" <%= address.isDefault ? 'checked' : '' %>>
                                                        <label class="form-check-label" for="isDefault<%= address._id %>">Set as Default</label>
                                                    </div>
                                                </div>
                                                <div class="text-end">
                                                    <button type="submit" class="btn btn-dark">Save Changes</button>
                                                </div>
                                            </form>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        <% }) %>
                    </div>
                <% } else { %>
                    <div class="profile-info mb-3">
                        <p class="text-muted">No addresses added yet.</p>
                    </div>
                <% } %>
            </div>
        </div>
    </div>
</div>

<br><br><br>

<footer class="footer">
    <%- include('../partials/user/footer') %>
</footer>

<script>
    <% if (message) { %>
        new Noty({
            type: '<%= message.type || "success" %>',
            layout: 'topRight',
            text: '<%= message.text %>',
            timeout: 3000,
            theme: 'bootstrap-v4'
        }).show();
    <% } %>


    document.getElementById('profileImage').addEventListener('change', function (e) {
            const file = e.target.files[0];
            const form = document.getElementById('profileImageForm');
            const allowedTypes = ['image/jpeg', 'image/jpg', 'image/png', 'image/gif'];

            if (file && !allowedTypes.includes(file.type)) {
                e.target.value = ''; // Clear the input
                Swal.fire({
                    icon: 'error',
                    title: 'Invalid File Type',
                    text: 'Please select a valid image file (PNG, JPEG, JPG, or GIF).',
                    confirmButtonColor: '#222',
                    confirmButtonText: 'OK'
                });
                return;
            }

            // Submit the form programmatically
            if (file) {
                form.submit();
            }
        });

        // Handle form submission response (optional, if you want to handle redirects or errors via AJAX)
        document.getElementById('profileImageForm').addEventListener('submit', async function (e) {
            e.preventDefault(); // Prevent default form submission for AJAX handling (optional)

            const formData = new FormData(this);
            try {
                const response = await fetch('/update-profile-image', {
                    method: 'POST',
                    body: formData
                });

                // Since the server renders the page, we expect a redirect
                if (response.ok) {
                    window.location.href = '/profile'; // Redirect to profile page
                } else {
                    const result = await response.json(); // If server sends JSON error
                    Swal.fire({
                        icon: 'error',
                        title: 'Error',
                        text: result.message || 'Failed to upload profile image.',
                        confirmButtonColor: '#222',
                        confirmButtonText: 'OK'
                    });
                }
            } catch (error) {
                console.error('Error uploading profile image:', error);
                Swal.fire({
                    icon: 'error',
                    title: 'Error',
                    text: 'An unexpected error occurred while uploading the image.',
                    confirmButtonColor: '#222',
                    confirmButtonText: 'OK'
                });
            }
        });


        function copyReferralLink() {
            const referralLink = document.getElementById('referralLink');
            referralLink.select();
            document.execCommand('copy');
            
            new Noty({
                type: 'success',
                layout: 'topRight',
                text: 'Referral link copied to clipboard!',
                timeout: 2000,
                theme: 'bootstrap-v4'
            }).show();
        }

        function shareViaWhatsApp() {
            const referralLink = document.getElementById('referralLink').value;
            const message = `Join using my referral link and get a special discount! ${referralLink}`;
            window.open(`https://wa.me/?text=${encodeURIComponent(message)}`, '_blank');
        }

        function shareViaEmail() {
            const referralLink = document.getElementById('referralLink').value;
            const subject = 'Join with my referral link';
            const body = `Hi,\n\nJoin using my referral link and get a special discount!\n\n${referralLink}\n\nThanks!`;
            window.location.href = `mailto:?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(body)}`;
        }

    async function copyReferralCode() {
        try {
            // Get the referral code element
            const referralCodeElement = document.querySelector('.referral-code');
            if (!referralCodeElement) {
                throw new Error('Referral code element not found in DOM');
            }

            // Extract and validate the referral code
            let referralCode = referralCodeElement.textContent.trim();
            if (!referralCode) {
                throw new Error('Referral code is empty or not rendered');
            }
            if (typeof referralCode !== 'string') {
                referralCode = String(referralCode);
            }
            const codePattern = /^[A-Z0-9]{8}$/;
            if (!codePattern.test(referralCode)) {
                throw new Error('Invalid referral code format');
            }

            // Attempt to copy using the Clipboard API
            if (navigator.clipboard && window.isSecureContext) {
                try {
                    await navigator.clipboard.writeText(referralCode);
                    showSuccessNotification('Referral code copied to clipboard!');
                } catch (clipError) {
                    console.warn('Clipboard API failed, falling back to execCommand:', clipError);
                    // Fallback to execCommand
                    await fallbackCopy(referralCode);
                }
            } else {
                // Fallback for non-secure contexts or unsupported browsers
                await fallbackCopy(referralCode);
            }
        } catch (err) {
            console.error('Failed to copy referral code:', err);
            showErrorNotification(`Failed to copy referral code: ${err.message}`);
        }
    }

    // Fallback function using execCommand
    function fallbackCopy(text) {
        return new Promise((resolve, reject) => {
            try {
                const tempInput = document.createElement('input');
                tempInput.style.position = 'absolute';
                tempInput.style.left = '-9999px';
                tempInput.value = text;
                document.body.appendChild(tempInput);
                tempInput.focus();
                tempInput.select();
                tempInput.setSelectionRange(0, 99999); // For mobile devices
                const success = document.execCommand('copy');
                document.body.removeChild(tempInput);
                if (!success) {
                    throw new Error('Fallback copy method failed');
                }
                showSuccessNotification('Referral code copied to clipboard!');
                resolve();
            } catch (err) {
                reject(err);
            }
        });
    }

    // Helper function to show success notification
    function showSuccessNotification(message) {
        new Noty({
            type: 'success',
            layout: 'topRight',
            text: message,
            timeout: 2000,
            theme: 'bootstrap-v4'
        }).show();
    }

    // Helper function to show error notification
    function showErrorNotification(message) {
        new Noty({
            type: 'error',
            layout: 'topRight',
            text: message,
            timeout: 3000,
            theme: 'bootstrap-v4'
        }).show();
    }

    // Edit Address Forms
    document.querySelectorAll('.editAddressForm').forEach(form => {
        const modalId = form.closest('.modal').id;
        const modal = document.getElementById(modalId);
        let originalValues = {};

        // Store original values when modal is shown
        modal.addEventListener('show.bs.modal', function() {
            form.querySelectorAll('input, select').forEach(input => {
                originalValues[input.name] = input.type === 'checkbox' ? input.checked : input.value;
            });
        });

        form.addEventListener('submit', async function(e) {
            e.preventDefault();
            const addressId = this.action.split('/').pop();
            const formData = new FormData(this);
            const jsonData = Object.fromEntries(formData);

            // Clear previous validation feedback
            form.querySelectorAll('.form-control').forEach(field => {
                field.classList.remove('is-invalid');
            });
            form.querySelectorAll('.invalid-feedback').forEach(feedback => {
                feedback.style.display = 'none';
            });

            // Check for changes
            let hasChanges = false;
            Object.keys(jsonData).forEach(key => {
                const currentValue = key === 'isDefault' ? (jsonData[key] === 'on') : jsonData[key];
                const originalValue = key === 'isDefault' ? originalValues[key] : originalValues[key];
                if (currentValue !== originalValue) hasChanges = true;
            });

            if (!hasChanges) {
                await Swal.fire({
                    icon: 'info',
                    title: 'No Changes',
                    text: 'No changes were detected in the address',
                    confirmButtonColor: '#222',
                    confirmButtonText: 'OK'
                });
                return;
            }

            try {
                const response = await fetch(`/address/edit/${addressId}`, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({
                        ...jsonData,
                        isDefault: jsonData.isDefault === 'on'
                    })
                });

                const result = await response.json();

                if (response.ok) {
                    if (result.noChanges) {
                        await Swal.fire({
                            icon: 'info',
                            title: 'No Changes',
                            text: 'No changes were detected in the address',
                            confirmButtonColor: '#222',
                            confirmButtonText: 'OK'
                        });
                    } else {
                        await Swal.fire({
                            icon: 'success',
                            title: 'Success!',
                            text: 'Address updated successfully',
                            confirmButtonColor: '#222',
                            confirmButtonText: 'OK'
                        }).then((result) => {
                            if (result.isConfirmed) {
                                window.location.reload();
                            }
                        });
                    }
                } else if (result.errors) {
                    // Show field-specific errors
                    Object.entries(result.errors).forEach(([field, message]) => {
                        const fieldElement = form.querySelector(`[name="${field}"]`);
                        const feedbackElement = fieldElement?.nextElementSibling;
                        if (fieldElement && feedbackElement) {
                            fieldElement.classList.add('is-invalid');
                            feedbackElement.textContent = message;
                            feedbackElement.style.display = 'block';
                        }
                    });
                    // Show SweetAlert with combined errors
                    const errorMessages = Object.values(result.errors).join('\n');
                    await Swal.fire({
                        icon: 'error',
                        title: 'Validation Error',
                        text: errorMessages,
                        confirmButtonColor: '#222',
                        confirmButtonText: 'OK'
                    });
                } else {
                    throw new Error(result.message || 'Failed to update address');
                }
            } catch (error) {
                console.error('Error:', error);
                await Swal.fire({
                    icon: 'error',
                    title: 'Error',
                    text: error.message || 'An unexpected error occurred while updating the address',
                    confirmButtonColor: '#222',
                    confirmButtonText: 'OK'
                });
            }
        });
    });


    // SweetAlert Confirm for Delete Address
    document.querySelectorAll('form[action^="/address/delete/"]').forEach(form => {
        form.addEventListener('submit', async function (e) {
            e.preventDefault(); // prevent default form submission

            const addressId = this.action.split('/').pop();

            const confirmDelete = await Swal.fire({
                title: 'Are you sure?',
                text: 'This address will be permanently deleted.',
                icon: 'warning',
                showCancelButton: true,
                confirmButtonColor: '#d33',
                cancelButtonColor: '#3085d6',
                confirmButtonText: 'Yes, delete it!',
                cancelButtonText: 'Cancel'
            });

            if (confirmDelete.isConfirmed) {
                try {
                    const response = await fetch(`/address/delete/${addressId}`, {
                        method: 'POST', // your route expects POST
                        headers: { 'Content-Type': 'application/json' }
                    });

                    const result = await response.json();

                    if (response.ok) {
                        await Swal.fire({
                            icon: 'success',
                            title: 'Deleted!',
                            text: result.message || 'Address deleted successfully.',
                            confirmButtonColor: '#222',
                            confirmButtonText: 'OK'
                        });
                        window.location.reload();
                    } else {
                        throw new Error(result.error || 'Failed to delete address');
                    }
                } catch (error) {
                    console.error('Error deleting address:', error);
                    await Swal.fire({
                        icon: 'error',
                        title: 'Error',
                        text: error.message || 'An unexpected error occurred while deleting the address',
                        confirmButtonColor: '#222',
                        confirmButtonText: 'OK'
                    });
                }
            }
        });
    });

</script>